name: Successful Build Test

on:
  push:
    branches: [ build_failure_test ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say hello
        run: echo "✅ Build succeeded!"
